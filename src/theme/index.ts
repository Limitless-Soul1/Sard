// theme — semantic token system. One source fans out to (a) :root CSS vars for the UI
// and (b) injected CSS for the book. Themes = day/night = "override book color".
// (placeholder — RAWY-05 skeleton)
export {};
