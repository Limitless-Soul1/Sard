// reader-engine — integration layer between React and foliate-js (in the WebView).
// Will host FoliateController (open/paginate/navigate), injectedCss (theme + the
// validated zoom-based text scaling, D6), and locator/CFI helpers.
// (placeholder — RAWY-05 skeleton)
export {};
