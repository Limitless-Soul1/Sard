# RAWY-105: the bundled piper engine (binaries + espeak config/data) must never have line
# endings converted — CRLF would corrupt espeak-ng data files and break phonemization.
src-tauri/resources/piper/** -text
